// TokenFetcher removed in production build to avoid committing tokens.
// Kept as an inert stub so imports won't break in older branches.

'use client';
const TokenFetcher = () => null;

export default TokenFetcher;