[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Add this to handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200